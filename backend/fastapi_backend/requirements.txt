# ExamEcho AI Service - Python dependencies
# Tested with Python 3.10+

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Configuration
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# LLM / LangChain (Groq cloud backend)
langchain>=0.2.0
langchain-groq>=0.3.0
groq>=0.10.0
httpx>=0.27.0

# STT / audio
# transformers and torch are optional and only needed for legacy compatibility aliases
# transformers>=4.40.0
# torch>=2.2.0

# Audio processing
soundfile>=0.12.1
webrtcvad>=2.0.10
numpy>=1.26.0
# ffmpeg must be installed on the host (see README)

# MCQ evaluation
sentence-transformers>=3.0.0

# File uploads
python-multipart>=0.0.9
